--- 
- 
  hosts: localhost
  name: "Hello World!"
  tasks: 
    - 
      name: "Installing pip modules for using vault lookup"
      pip: "name={{item}}"
      with_items: 
        - hvac
        - "hvac[parser]"
    - 
      name: "Hello World!"
      shell: "echo \"Hi! Tower is working! version 3\""
    - 
      debug: 
        msg: "{{ data | default('default_value') }} {{ akeyless | default('default_value2') }} {{ KEY | default('default_value2') }} {{ key | default('default_value2') }} {{ data | default('default_value2') }}"
      name: "What's my name again?" 
