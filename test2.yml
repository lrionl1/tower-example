-
    hosts: localhost
    name: 'Hello World!'
    environment:
        VAULT_ADDR: 'http://host.docker.internal:8200'
        VAULT_TOKEN: 'p-xcn1596fg6oh..krGccA91+MD2K6/xmWhyS4PhcYde5AA3CvPA2O9bDdI'
    tasks:
        -
            debug: msg="{{  lookup('vault', 'secret/test') }} | default('default_value') }}"
